# Narzędzia wspierające programowanie
## Temat: CMake
---

### Ćwiczenie 5.

Zmodyfikuj kod źródłowy i plik `CMakeLists.txt` z ćwiczenia 4. tak, aby wartości przekazywane funkcji `Print` w funkcji `main` (w obu jej wywołaniach) mogły być określane przez użytkownika w wierszu poleceń w czasie budowania projektu.