# Narzędzia wspierające programowanie
## Temat: CMake
---

### Ćwiczenie 4.

Zmodyfikuj kod źródłowy i plik `CMakeLists.txt` z ćwiczenia 3. tak, aby wartości przekazywane funkcji `Print` w funkcji `main` (w obu jej wywołaniach) nie były określone w kodzie źródłowym, a w pliku `CMakeLists.txt`.